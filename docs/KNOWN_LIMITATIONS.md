# محدودیت‌های صریح نسخهٔ اول

- مخزن از ابتدا خالی بود؛ این commit اولین کد پروژه است.
- محیط فعلی Flutter SDK و Android/iOS SDK ندارد. هستهٔ Dart با `dart analyze`
  و `dart test` قابل بررسی است، اما build واقعی Android و iOS تا نصب ابزارهای
  رسمی آن پلتفرم‌ها قابل ادعا نیست.
- رابط Flutter، پایگاه دادهٔ محلی رمزگذاری‌شده، اعلان‌های سیستم، backup و
  provider اختیاری Local AI در مراحل بعدی روی همین هسته اضافه می‌شوند.
- مدل مالی عمداً داده‌ای را به cloud نمی‌فرستد و هنوز هیچ provider ابری در
  کد وجود ندارد.